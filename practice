import streamlit as st

def main():
    st.title('Input and Display Example')
    
    # Text input box
    user_input = st.text_input('Enter some text:')
    
    # Display the entered text
    st.write('You entered:', user_input)

if __name__ == '__main__':
    main()
